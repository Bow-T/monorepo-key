/// Engine gõ tiếng Việt (Telex/VNI) thuần Dart.
///
/// Đây là "bộ não" dùng chung: app Flutter (UI cài đặt / gõ thử) import trực tiếp,
/// và nó cũng là SPEC CHUẨN mà các bộ gõ native (macOS/Windows/Android/iOS) bám theo.
library;

export 'src/viet_model.dart';
export 'src/engine.dart' show VietEngine;
